# Example environment variables for NeoCal AI backend
# Copy this to .env and fill in values locally (DO NOT commit your real keys).

# OpenAI API key (required for vision-based image parsing if you want to use OpenAI)
OPENAI_API_KEY=

# Optional: HuggingFace API key (alternative to OpenAI)
HUGGINGFACE_API_KEY=

# Which OpenAI vision model to use (fallbacks exist)
OPENAI_VISION_MODEL=gpt-4-vision-preview

# DB (optional) - default uses SQLite file in `database/`
DATABASE_URL=sqlite:///./neocal.db
